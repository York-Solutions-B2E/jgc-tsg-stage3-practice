# TSG Practice (weeks 5 and 6)

This is a simple attempt to string together Postgres and Kafka with Spring Boot.

## How to run

In a bash terminal, run: `./start-back.sh`

To export the output to a log file, specify the file name: `./start-back.sh logfile.log`
